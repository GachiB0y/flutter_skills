# 📚 Make Commands Reference

Справочник по командам Makefile для single package архитектуры.

---

## 🚀 Основные команды

### `make help`

Показывает все доступные команды.

```bash
make help
```

---

## 📦 Управление зависимостями

### `make get`

Установка Flutter зависимостей из `pubspec.yaml`.

```bash
make get
```

Эквивалент:

```bash
fvm flutter pub get
```

### `make bootstrap`

Инициализация проекта после клонирования. Выполняет:

1. Установку зависимостей (`pub get`)
2. Генерацию кода (`build_runner`)
3. Проверку кода (`analyze`)

```bash
make bootstrap
```

**Когда использовать:**

- После первого клонирования репозитория
- После переключения на другую ветку с изменениями в зависимостях
- Для полной переинициализации проекта

---

## 🔨 Генерация кода

### `make gen`

Однократная генерация Dart файлов через build_runner.

```bash
make gen
```

Эквивалент:

```bash
fvm dart run build_runner build -d
```

**Когда использовать:**

- После добавления/изменения моделей с аннотациями
- После изменения файлов с freezed, json_serializable, retrofit

### `make gen-watch`

Автоматическая генерация при изменении файлов.

```bash
make gen-watch
```

Эквивалент:

```bash
fvm dart run build_runner watch -d
```

**Когда использовать:**

- Во время активной разработки с частыми изменениями моделей
- Оставить работающим в фоне

### `make gen-clean`

Очистка и полная перегенерация файлов (удаляет конфликтующие файлы).

```bash
make gen-clean
```

Эквивалент:

```bash
fvm dart run build_runner build --delete-conflicting-outputs
```

**Когда использовать:**

- При конфликтах сгенерированных файлов
- После крупных рефакторингов

---

## 🔍 Проверка кода

### `make analyze`

Статический анализ кода.

```bash
make analyze
```

Эквивалент:

```bash
fvm flutter analyze
```

**Проверяет:**

- Ошибки типов
- Потенциальные баги
- Нарушения правил линтера

### `make autofix`

Автоматическое исправление проблем.

```bash
make autofix
```

Эквивалент:

```bash
fvm dart fix --apply
```

**Исправляет:**

- Устаревший синтаксис
- Простые lint warnings
- Deprecated API usage

### `make format`

Форматирование Dart кода.

```bash
make format
```

Запускает скрипт `scripts/format.bash`, который форматирует все `.dart` файлы.

---

## 🧪 Тестирование

### `make test`

Запуск всех тестов проекта.

```bash
make test
```

Запускает скрипт `scripts/test.bash`.

### `make test-coverage`

Запуск тестов с генерацией отчета о покрытии.

```bash
make test-coverage
```

Эквивалент:

```bash
fvm flutter test --coverage
```

**Результат:**

- Отчет о покрытии в `coverage/lcov.info`
- Можно визуализировать с помощью инструментов (genhtml, lcov)

---

## 🚀 Запуск приложения

### `make run`

Запуск приложения в debug режиме.

```bash
make run
```

Эквивалент:

```bash
fvm flutter run
```

**Для выбора устройства:**

```bash
fvm flutter devices  # Посмотреть доступные устройства
fvm flutter run -d <device_id>
```

### `make run-release`

Запуск приложения в release режиме (оптимизированная сборка).

```bash
make run-release
```

Эквивалент:

```bash
fvm flutter run --release
```

---

## 📱 Сборка

### `make build-android`

Сборка Android APK (release).

```bash
make build-android
```

Эквивалент:

```bash
fvm flutter build apk --release
```

**Результат:**

- APK файл в `build/app/outputs/flutter-apk/`

### `make build-ios`

Сборка iOS приложения (release).

```bash
make build-ios
```

Эквивалент:

```bash
fvm flutter build ios --release
```

**Требования:**

- macOS
- Xcode
- Apple Developer сертификаты

---

## 📁 Создание модулей

### `make create-feature NAME=<name>`

Создание нового feature модуля.

```bash
make create-feature NAME=profile
```

**Создает структуру:**

```
lib/src/feature/profile/
├── presentation/
│   ├── bloc/
│   └── widget/
├── domain/
│   ├── models/
│   └── repositories/
├── data/
│   ├── models/
│   ├── repositories/
│   └── datasources/
└── dependencies/

test/src/feature/profile/
├── presentation/bloc/
├── domain/
└── data/
```

**Также создает:**

- Шаблон BLoC с events и states
- Шаблон экрана
- README с инструкциями

**Примеры:**

```bash
make create-feature NAME=profile
make create-feature NAME=notifications
make create-feature NAME=chat
```

### `make create-core NAME=<name>`

Создание нового core модуля.

```bash
make create-core NAME=cache
```

**Создает структуру:**

```
lib/src/core/cache/
├── cache.dart           # Interface
├── cache_impl.dart      # Implementation
└── README.md

test/src/core/cache/
└── cache_test.dart
```

**Примеры:**

```bash
make create-core NAME=cache
make create-core NAME=encryption
make create-core NAME=notifications_service
```

---

## 🧹 Очистка

### `make clean`

Очистка build файлов.

```bash
make clean
```

Запускает скрипт `scripts/clean.bash`.

**Удаляет:**

- `build/` директорию
- Временные файлы

### `make clean-all`

Глубокая очистка (включая pub cache).

```bash
make clean-all
```

**Удаляет:**

- `build/`
- `.dart_tool/`
- Pub cache для проекта

**После этого нужно:**

```bash
make get  # Восстановить зависимости
```

---

## 🔗 Цепочки команд

### После клонирования проекта

```bash
make bootstrap
```

### Обычный рабочий процесс

```bash
# Начало работы
make get
make gen-watch  # Оставить работать в фоне

# В другом терминале
make run
```

### Перед коммитом

```bash
make format
make analyze
make test
```

### После изменения моделей

```bash
make gen
make test
```

### Создание нового feature

```bash
make create-feature NAME=new_feature
# Редактировать файлы
make gen
make test
```

---

## 💡 Лучшие практики

### ✅ DO

- Запускай `make analyze` перед коммитом
- Используй `make gen-watch` во время разработки моделей
- Запускай `make test` перед push
- Используй `make bootstrap` после переключения веток
- Форматируй код через `make format`

### ❌ DON'T

- Не коммить несгенерированные файлы (.g.dart)
- Не пропускать `make analyze` warnings
- Не забывать про `make test`

---

## 🆘 Troubleshooting

### Проблема: "Conflicting outputs"

**Решение:**

```bash
make gen-clean
```

### Проблема: "Package not found"

**Решение:**

```bash
make clean-all
make get
```

### Проблема: Старые зависимости

**Решение:**

```bash
make clean-all
make bootstrap
```

### Проблема: Тесты не находят файлы

**Решение:**

```bash
make gen
make test
```

---

## 📋 Полный список команд

| Команда               | Описание                 |
| --------------------- | ------------------------ |
| `make help`           | Показать все команды     |
| `make get`            | Установить зависимости   |
| `make bootstrap`      | Инициализация проекта    |
| `make gen`            | Сгенерировать код        |
| `make gen-watch`      | Генерация в режиме watch |
| `make gen-clean`      | Очистить и сгенерировать |
| `make analyze`        | Анализ кода              |
| `make autofix`        | Автоисправление          |
| `make format`         | Форматирование           |
| `make test`           | Запуск тестов            |
| `make test-coverage`  | Тесты с покрытием        |
| `make run`            | Запуск (debug)           |
| `make run-release`    | Запуск (release)         |
| `make build-android`  | Сборка Android           |
| `make build-ios`      | Сборка iOS               |
| `make create-feature` | Создать feature          |
| `make create-core`    | Создать core модуль      |
| `make clean`          | Очистка                  |
| `make clean-all`      | Глубокая очистка         |

---

## 🔗 См. также

- [Архитектура проекта](../.github/instructions/architecture/architecture-project.md)
- [Feature модули](../.github/instructions/modules/feature-modules.md)
- [Core модули](../.github/instructions/modules/core-modules.md)

---

**Версия:** 2.0 (Single Package)  
**Обновлено:** 23.12.2025
